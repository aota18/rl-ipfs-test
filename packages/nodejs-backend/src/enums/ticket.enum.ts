export enum TicketStatus {
  CREATED = 'CREATED',
  USED = 'USED',
  EXPIRED = 'EXPIRED',
}

export enum TicketType {
  FREE = 'FREE',
  PAID = 'PAID',
}

export enum PermitStatus {
  CREATED = 'CREATED',
  REQUESTED = 'REQUESTED',
  APPROVED = 'APPROVED',
  DENIED = 'DENIED',
}
